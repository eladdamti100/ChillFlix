#ifndef MOVIES_H
#define MOVIES_H

#include <iostream>
#include <fstream>
#include <string>
#include <set>
using namespace std;

class Movies {
public:
    bool isMovieValid(long int movieID); // Function to check if movie valid TODO (remove it / change to only get positive number?)
};

#endif // MOVIES_H
